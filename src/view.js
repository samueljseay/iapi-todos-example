/**
 * WordPress dependencies
 */
import { store, getContext } from '@wordpress/interactivity';

const { state } = store( 'iapi-todos', {
	actions: {
		// Add some magic here!
	},
	callbacks: {
		// Add some magic here!
	},
} );
